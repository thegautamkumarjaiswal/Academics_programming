/* Casual program on mathmatical expression */
#include<stdio.h>
#include<conio.h>
void main()
{
    int a=3, b=2, x=5, y=4, ans;
    /* answer the function */
    ans = 5*b*b*x-3*a*y*y-8*b*b*x+10*a*y;
    printf("%d", ans);
}